#include "main.h"
#include <stdio.h>

/**
 * print_last_digit - check lower case letter
 * @c: character
 * Return: Always 0 (Success)
 */

int print_last_digit(int c)
{
	int last_digit = c % 10;
	return last_digit;
}
